let num_a = 10;
let num_b = 20;
let num_c = num_a + num_b;

let soma = (`${num_a} + ${num_b} = ${num_c}`);

console.log(soma);

num_c = num_a - num_b;

console.log(`${num_a} - ${num_b} = ${num_c}`);

num_c = num_a / num_b;

console.log(`${num_a} / ${num_b} = ${num_c}`);

num_c = num_a * num_b;

console.log(`${num_a} * ${num_b} = ${num_c}`);